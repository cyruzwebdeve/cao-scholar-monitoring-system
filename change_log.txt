2025-01-23 | UI Redesign - Application form completely transformed to match reference design. Replaced header-based step tabs with left sidebar navigation. Added progress counter (0/3), info text "Start filling out the form below", and warning box for required fields. Changed button labels from "Back/Next/Submit" to "Previous/Save & Continue". Removed 4th review step - form now has 3 steps (Personal, Address & Academic, Family & Eligibility). Sidebar buttons highlight in green when active, show step number/name. Added success screen modal with reference number display after submission. Updated CSS with sidebar layout, button styling, form warnings, and progress tracking. All navigation features tested and working.

2026-07-02 | Added backend and frontend validation for application form inputs, including uppercase name enforcement, mobile number length validation, and inline field error hints. Verified validator response and frontend build.
2026-07-10 | Fixed React auth state restoration in the frontend by initializing authentication state from localStorage during app startup instead of updating state inside an effect, preventing the setState-in-effect warning.
2026-07-10 | Refreshed the frontend application form to match the provided screenshots: replaced the hero/info layout with a dark top header, centered form stage, three-step sidebar, card-based form header, sidebar warning, financial/eligibility grid, and mobile-responsive spacing. Updated frontend/src/ApplicationPage.jsx, frontend/src/components/ApplicationForm.jsx, and frontend/src/index.css; validated with npm run build.
2026-07-10 | Moved the application page header flush to the top of the body by removing the application shell top padding in frontend/src/index.css. Validated the edited stylesheet slice.
2026-07-10 | Expanded the application shell and form stage to full viewport width by removing the inset horizontal page padding and letting the stage span 100% width. Validated the edited stylesheet slice.
2026-07-10 | Narrowed the application width change so only the header spans the viewport; restored the form stage to a centered max-width layout in frontend/src/index.css. Validated the edited stylesheet slice.
2026-07-10 | Compressed the application form contents a little toward the center by narrowing and centering the form container in frontend/src/index.css. Validated the edited stylesheet slice.
2026-07-10 | Compressed the application form layout further to match the reference by reducing the container max-width, side padding, and sidebar-to-form gap in frontend/src/index.css. Validated the edited stylesheet slice.
2026-07-10 | Tightened the spacing between form fields and section blocks in frontend/src/index.css by reducing row gaps, input padding, and message spacing to better match the screenshot density. Validated the edited stylesheet slice.
2026-07-10 | Updated the sidebar progress card to match the screenshot with a compact header row, slim progress bar, and helper text. Validated the edited JSX and stylesheet slices.
2026-07-10 | Compressed the header content and aligned it to the same centered width as the form by adding a header inner wrapper in frontend/src/ApplicationPage.jsx and frontend/src/index.css. Validated the edited JSX and stylesheet slices.
2026-07-10 | Made the form card header a little grey-ish by tinting the header background and softening the section number and title colors in frontend/src/index.css. Validated the edited stylesheet slice.
2026-07-10 | Integrated the existing municipality.json and brgy.json data into the application form as dependent dropdowns for Municipality and Barangay, storing codes in state and resolving names on submit. Validated with a frontend build.
2026-07-10 | Integrated schools_list.json into the application form so the School field uses the JSON-backed dropdown list. Validated with a frontend build.
2026-07-12 | Updated frontend/src/components/ApplicationForm.jsx to render required field markers through a reusable FieldLabel component, and added frontend/src/index.css styling so the asterisk renders in red for every required field label. Change log updated per project preference.
2026-07-12 | Added a role-aware super admin dashboard by making the sidebar sections depend on role, wiring Dashboard to section-specific system views (applications, scholars, exam/results, payroll, announcements, user roles, reports, settings), and resetting the active section on role changes. Validated with a frontend production build.
2026-07-12 | Moved the Super Admin Access badge out of the dashboard hero and into the dashboard page header so the report hero only shows its export badge. Validated the updated frontend build after the layout change.
2026-07-12 | Removed the role pill from the dashboard hero so the super admin command center header only keeps the primary hero badge. Change logged per project preference.
2026-07-12 | Removed the unused Dashboard user prop after deleting the hero role pill, keeping the super admin dashboard component clean. Change logged per project preference.
2026-07-12 | Removed the dashboard hero Live badge from the super admin command center and made hero badges render only when a section provides one. Change logged per project preference.
2026-07-12 | Removed the entire dashboard hero block from the super admin command center so the page now opens directly into the dashboard metrics and panels. Change logged per project preference.
2026-07-12 | Centered the sidebar brand block in the admin sidebar header so the logo and workspace title sit in the middle of the top section. Change logged per project preference.
2026-07-12 | Changed the sidebar brand alignment from centered to middle-aligned left layout so the logo and title sit vertically centered without shifting the whole block to the middle. Change logged per project preference.
2026-07-12 | Reduced the dashboard header height by tightening the padding, green strip height, badge padding, and title spacing in frontend/src/index.css. Change logged per project preference.
2026-07-12 | Replaced the super admin sidebar sections with the requested set: Dashboard, Applicants, Staff, Examination Management, Results Management, Scholars, Billing, Payroll, Announcements, School Catalog, Activity Logs, Academic Years, Reports, and Settings. Also mapped the dashboard content to matching section views. Change logged per project preference.
2026-07-13 | Replaced the frontend icon usage with lucide-react in frontend/src/ApplicationPage.jsx, frontend/src/components/ApplicationForm.jsx, and frontend/src/Dashboard.jsx. Swapped the application page, form header, submitted state, warning badge, and dashboard metric icons to Lucide components; left the current layout and styling intact. Change logged per project preference.
2026-08-16 | Prepared the capstone for production deployment on Vercel and Render. Added a Vercel SPA configuration, Render Blueprint, Prisma Postgres baseline migration and production bootstrap, pooled/runtime and direct/migration database handling, restricted production CORS, security headers, API rate limits, a database-aware health endpoint, graceful shutdown, and Node version constraints. Moved production scholar documents to private Vercel Blob storage and announcement images to public Vercel Blob storage while preserving local fallback behavior. Replaced runtime manual primary-key allocation with database auto-increment IDs. Added a deployment guide and environment templates. Verified the isolated migration with no schema drift, frontend build and lint, backend syntax, local API/database health, Prisma schema, and zero npm audit vulnerabilities.
2026-08-16 | Hardened the production administrator bootstrap cleanup flow. Render deployments now safely skip administrator creation whenever the temporary BOOTSTRAP_ADMIN_PASSWORD is absent, allowing the password to be removed after first-account creation while retaining BOOTSTRAP_ADMIN_EMAIL.
2026-08-16 | Added secure one-time production bootstrap support for Billing/Payroll Admin and Content Moderator accounts. Each role uses separate Render email, optional name, and temporary password variables; passwords must be at least 12 characters, are hashed before storage, are never logged, and can be removed after account creation without affecting future deployments.
2026-08-16 | Created a responsive public landing page for PGCEAP with institutional branding, clear application and portal entry points, a scholarship journey overview, applicant guidance, common questions, and Community Affairs Office support information. Changed the root route to the public homepage while preserving role-based authenticated destinations and all existing application, login, examination, and dashboard routes. Added an optimized landing image and route-level code splitting so public visitors do not need to load dashboard code before viewing the homepage.
2026-08-16 | Completed an application-wide responsive layout pass for public, application, examination, applicant, scholar, and administrator pages. Added a mobile admin navigation drawer with backdrop, scroll locking, and Escape-key support; reflowed dashboard metrics, filters, transfer boards, forms, tables, drawers, and modals across tablet and phone breakpoints; and optimized the examination-taking interface for smaller screens. Verified with frontend lint, production build, stylesheet checks, and desktop/tablet/mobile browser captures.
2026-08-16 | Removed the mobile application-page shell inset so the application header attaches cleanly to both viewport edges, while preserving internal header and form spacing.
2026-08-16 | Reordered the responsive login page so the institutional introduction appears first and the login form follows beneath it on tablet and mobile screens, while preserving the desktop two-column layout.
2026-08-16 | Corrected uneven mobile dashboard gutters by replacing the desktop flex sizing with a block-level mobile shell, allowing the main content width and equal 16px side padding to remain inside the viewport.
2026-08-16 | Centered every mobile admin section with an explicit viewport gutter: dashboard content no longer compounds nested horizontal padding, and each section uses a calculated width with automatic left and right margins for exact 16px alignment.
2026-08-16 | Removed the confirmed 12px rightward shift from the mobile admin shell by fully resetting its inherited responsive margin instead of overriding only the top margin. Browser measurements now place the shell at the viewport origin and preserve equal section gutters.
2026-08-16 | Connected Examination Management schedules to the backend and Results Management. Municipality schedules, date ranges, venues, activation state, and academic year are now persisted in PostgreSQL; result rows automatically resolve the applicant's scheduled municipality examination date even before an exam submission exists, with existing browser data retained as an offline/transition fallback.
2026-08-16 | Reworked the post-application success screen for mobile. Applicant control number, email, and one-time temporary password now appear in a structured login-details panel in production; the success page scrolls on short screens, uses compact phone spacing and full-width controls, wraps long credentials safely, and allows 30 seconds before redirecting to login. Verified with frontend lint, production build, and a local mobile-width browser capture.
2026-08-16 | Split the 14,249-line global stylesheet into shared foundations and lazy-loaded landing, login, application, applicant, scholar, administrator, and examination style modules. Reduced the initial production CSS bundle from 258.85 KB (44.76 KB gzip) to 41.23 KB (9.28 KB gzip), removed the duplicate App-level global import, corrected relocated asset paths, and preserved page-specific cascade order. Verified with lint, production build, six public desktop/mobile captures, and mocked admin/applicant/scholar responsive captures without accessing production data.
2026-08-16 | Fixed the local Prisma client/datasource mismatch by regenerating the application client with its standard PostgreSQL query engine and added a predev generation step so `npm run dev` refreshes the client automatically.
2026-08-17 | Connected applicant examination announcements to persisted examination schedules. The applicant API now resolves the schedule by municipality and active school year, while the dashboard displays the activated date and venue or a pending “wait for schedule” notice instead of relying on another browser’s local storage.
2026-08-17 | Added an idempotent production migration to ensure the exams table has the exam_end_date column required by multi-day schedule saving, and improved the save error log context.
2026-08-17 | Increased the examination schedule transaction wait and execution timeout to support saving all municipality schedules reliably on the remote database connection.
2026-08-20 | Added a Super Administrator-only System Health panel with lightweight frontend responsiveness and backend API/database latency graphs, sequential 30-second checks, manual refresh, and degraded or unavailable states. Enhanced the health endpoint to report database query latency.
2026-08-20 | Improved Lighthouse quality across the landing, login, and application routes with responsive WebP assets, mobile-specific login imagery, descriptive metadata, a valid robots file and sitemap, accessible contrast, and legible application text. Mobile verification reached 98-99 Performance and 100 Accessibility, Best Practices, and SEO under normal API conditions; the login transfer fell from 5,974 KiB to 124 KiB.
2026-08-20 | Added the Gmail lifecycle mailer for applicants. New applications receive their portal credentials, completed examinations receive a waiting-for-results confirmation, and approved applicants receive their scholar ID and next-step notice. Added branded HTML and plain-text templates, escaped dynamic content, Gmail App Password configuration, safe delivery failure handling that never rolls back saved records, a connection verification command, Render/deployment documentation, and automated mailer tests.
2026-08-20 | Replaced the production Gmail SMTP path with Gmail API delivery over HTTPS because Render Free blocks outbound SMTP ports. Added OAuth refresh-token handling with access-token caching, MIME-safe messages, the minimum gmail.send permission, SMTP fallback for compatible local or paid hosts, a PKCE-protected one-time desktop authorization helper, safe provider-stage diagnostics, Render environment placeholders, documentation, and API transport tests.
2026-08-20 | Fixed applicant examination schedule presentation across the guidance and announcement cards. ISO timestamps now render as consistent Philippine calendar dates and multi-day ranges, schedule labels and values have proper vertical spacing, and long venue values wrap safely.
2026-08-20 | Removed the Applicant Dashboard refresh flash of hardcoded application data by introducing explicit loading, failure, and retry states for the live application request. Replaced fake control-number, email, and 40%-complete fallbacks with honest unavailable or empty-record states, and redesigned Application Guidance as a compact responsive examination card with clear date, delivery mode, venue, and municipality hierarchy. Verified with frontend lint and production build.
2026-08-20 | Added secure Gmail-based Forgot Password functionality for applicants, scholars, and administrators. Users can request recovery by email or control number, receive a branded reset link, and set a new password through responsive public pages. Reset tokens are random, SHA-256 hashed in PostgreSQL, single-use, and expire after 30 minutes; generic request responses prevent account discovery, dedicated rate limits reduce abuse, and successful resets invalidate earlier login sessions. Added a production migration, deployment documentation, and automated token, mailer, and rate-limit coverage; verified Prisma generation/validation, backend tests, frontend lint, and production build.
2026-08-20 | Fixed municipality-based examination assignment consistency in Applicant Management. New applications are now assigned to an existing current-year municipality schedule, saving schedules bulk-assigns matching applicants with duplicate-safe inserts, and a production migration backfills existing matches. The Scheduled for Exam metric now counts unique current-year scheduled applicants, preassigned slots remain pending until attendance, and online submissions use only the applicant's active municipality examination. Updated the metric description and added automated assignment coverage.
2026-08-20 | Added a functional Super Administrator Activity Logs workspace and system audit trail. Successful applicant, scholar, and administrator sign-ins now atomically update their last-login timestamps and create audit events; successful authenticated write operations record safe action metadata without request bodies or credentials; new applications record submission events. Added protected filtering, pagination, actor resolution, live metrics, 30-second refresh, responsive audit presentation, and automated login/audit service coverage.
2026-08-20 | Replaced the Super Administrator Staff placeholder with a secure account-management workspace backed by the real administrators table. Super Administrators can search and filter staff, create Administrator, Billing/Payroll Admin, Content Moderator, or Super Administrator accounts, edit names, emails, roles, and active status, and set new passwords after confirming their own password. Access and password changes invalidate existing sessions; safeguards prevent self-demotion and removal of the final active Super Administrator; staff mutations are rate-limited and written to Activity Logs. Removed the broken legacy user-role endpoint, added responsive modal-based UX with no browser alerts, and verified 31 backend tests, frontend lint/build, and Prisma schema validity.
2026-08-20 | Corrected the login button's role hint so ordinary applicant Gmail addresses no longer display “Continue as Admin.” The cosmetic label now identifies an administrator only when the entered identifier contains the word “admin”; secure role resolution remains controlled by the backend account record.
